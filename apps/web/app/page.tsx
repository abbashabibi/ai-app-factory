"use client";

import { useEffect, useMemo, useState, type ReactNode } from "react";

type StageState = "done" | "active" | "waiting" | "error";
type Artifact = { name?: string; type?: string; url?: string };
type BuildInfo = { buildId: string; status?: string; finished?: boolean; failed?: boolean; artifacts?: Artifact[] };
type Project = { projectId: string; accountId: string; title: string; brief: string; stage: string; progress: number; createdAt: string; updatedAt: string; error: string | null; execution?: { status: string; lastError?: string | null; qa?: { passed: boolean }; build?: BuildInfo } };
type Stage = { key: string; title: string; state: StageState };
const stageDefs = [["IDEA", "تحلیل ایده"], ["RESEARCHED", "تحقیق و نیازمندی‌ها"], ["SCRIPTED", "طراحی و معماری"], ["ASSETS_READY", "طراحی UI/UX"], ["RENDERED", "تولید کد"], ["QA_PASSED", "تست و QA"], ["UPLOADED", "Build / APK"], ["ANALYZED", "تحویل نهایی"]] as const;
function StageIcon({ type }: { type: string }) { const c={fill:"none",stroke:"currentColor",strokeWidth:1.7,strokeLinecap:"round" as const,strokeLinejoin:"round" as const}; const p:Record<string,ReactNode>={IDEA:<><path {...c} d="M12 3a6 6 0 0 0-3.4 10.95c.8.55 1.4 1.35 1.4 2.35h4c0-1 .6-1.8 1.4-2.35A6 6 0 0 0 12 3Z"/><path {...c} d="M10 20h4M10.5 17h3"/></>,RESEARCHED:<><circle {...c} cx="10.5" cy="10.5" r="5.5"/><path {...c} d="m15 15 4 4"/></>,SCRIPTED:<><path {...c} d="M6 3.5h9l3 3V20H6z"/><path {...c} d="M15 3.5V7h3M9 11h6M9 14h6M9 17h4"/></>,ASSETS_READY:<><rect {...c} x="3.5" y="5" width="17" height="14" rx="2"/><path {...c} d="m6.5 16 4-4 2.5 2.5 2-2 2.5 3.5M8 9h.01"/></>,RENDERED:<><path {...c} d="M5 4h14v16H5zM9 8l6 4-6 4z"/></>,QA_PASSED:<><path {...c} d="M12 3 19 6v5c0 4.2-2.8 7.3-7 9-4.2-1.7-7-4.8-7-9V6z"/><path {...c} d="m8.5 12 2.2 2.2 4.8-5"/></>,UPLOADED:<><path {...c} d="M12 16V5M8 9l4-4 4 4M5 18v2h14v-2"/></>,ANALYZED:<><path {...c} d="M5 19V9M12 19V5M19 19v-7"/><path {...c} d="M3 19h18"/></>}; return <svg className="stage-svg" viewBox="0 0 24 24" aria-hidden="true">{p[type]}</svg>; }
function buildStages(project: Project|null): Stage[] { const current=project?stageDefs.findIndex(([k])=>k===project.stage):-1; return stageDefs.map(([key,title],i)=>({key,title,state:project?.error&&i===current?"error":i<current?"done":i===current?"active":"waiting"})); }
export default function Dashboard(){
 const apiBase=(process.env.NEXT_PUBLIC_API_BASE_URL||"http://localhost:3000").replace(/\/$/,"");
 const [project,setProject]=useState<Project|null>(null),[command,setCommand]=useState(""),[loading,setLoading]=useState(false),[connection,setConnection]=useState<"live"|"offline">("offline"),[elapsed,setElapsed]=useState(0),[message,setMessage]=useState(""),[artifactLink,setArtifactLink]=useState(""),[jobId,setJobId]=useState("");
 const stages=useMemo(()=>buildStages(project),[project]),progress=project?.progress??0,build=project?.execution?.build,active=stages.find(s=>s.state==="active");
 useEffect(()=>{const saved=localStorage.getItem("aif_project_id");if(saved)setProject({projectId:saved} as Project);},[]);
 useEffect(()=>{if(!project?.projectId)return;localStorage.setItem("aif_project_id",project.projectId);const poll=async()=>{try{const r=await fetch(`${apiBase}/api/v1/projects/${project.projectId}`,{cache:"no-store"});if(!r.ok)throw Error();setProject(await r.json());setConnection("live");}catch{setConnection("offline")}};poll();const t=window.setInterval(poll,3000);return()=>window.clearInterval(t)},[apiBase,project?.projectId]);
 useEffect(()=>{if(!project)return;const started=new Date(project.createdAt).getTime();const tick=()=>setElapsed(Math.max(0,Math.floor((Date.now()-started)/1000)));tick();const t=window.setInterval(tick,1000);return()=>window.clearInterval(t)},[project]);
 useEffect(()=>{if(build?.finished)setMessage("APK با موفقیت ساخته شد.");else if(build?.failed)setMessage("Build شکست خورد؛ خطا را بررسی کنید.");},[build?.finished,build?.failed]);
 async function post(path:string,body?:unknown){const r=await fetch(`${apiBase}${path}`,{method:"POST",headers:{"content-type":"application/json"},body:body===undefined?undefined:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d?.error||"REQUEST_FAILED");return d;}
 async function runFactory(){if(!command.trim()||loading)return;setLoading(true);setArtifactLink("");try{setMessage("Job پایدار ثبت شد؛ Worker مستقل در حال اجراست…");const created=await post("/api/v1/projects",{title:command.trim(),brief:command.trim()});setProject(created);setConnection("live");setCommand("");const job=await post("/api/v1/jobs",{projectId:created.projectId,kind:"factory"});setJobId(job.jobId||"");setMessage(`Job ${job.jobId||""} در صف قرار گرفت؛ صفحه را می‌توانید ببندید.`);}catch(e){setMessage(`خطا: ${e instanceof Error?e.message:"UNKNOWN_ERROR"}`)}finally{setLoading(false)}}
 async function makeArtifactLink(){if(!project?.projectId||!build?.buildId||!build.finished||!build.artifacts?.length)return;const a=build.artifacts.find(x=>x.url)||build.artifacts[0];try{const d=await post(`/api/v1/projects/${project.projectId}/build/${build.buildId}/artifact-url`,{artifactUrl:a.url,artifactName:a.name});setArtifactLink(d?.url||d?.publicUrl||"")}catch(e){setMessage(`خطای لینک APK: ${e instanceof Error?e.message:"UNKNOWN_ERROR"}`)}}
 const time=`${String(Math.floor(elapsed/3600)).padStart(2,"0")}:${String(Math.floor((elapsed%3600)/60)).padStart(2,"0")}:${String(elapsed%60).padStart(2,"0")}`;const canDownload=Boolean(build?.finished&&build.artifacts?.length);
 return <main className="shell"><aside className="sidebar"><div className="brand">✦ Abbas AI Factory</div><nav className="nav"><a className="active" href="#dashboard">داشبورد</a><a href="#projects">پروژه‌ها</a><a href="#videos">ویدئوها</a><a href="#channels">کانال‌های YouTube</a><a href="#analytics">تحلیل‌ها</a><a href="#license">لایسنس</a><a href="#settings">تنظیمات</a><a href="#help">راهنما</a></nav></aside><section className="main" id="dashboard"><header className="top"><div><div className="eyebrow">AI WORKSPACE / AUTONOMOUS FACTORY</div><h1>مرکز فرمان هوش مصنوعی</h1></div><span className={`badge ${connection==="live"?"live":"offline"}`}>● {connection==="live"?"LIVE":"OFFLINE"} · LIFETIME ACTIVE</span></header><section className="monitor card full"><div className="monitor-head"><div><div className="label">AUTONOMOUS MONITORING</div><h2>{project?.title||"پروژه جدید"}</h2></div><div className="progress-number">{progress}%</div></div><div className="progress"><span style={{width:`${progress}%`}}/></div><div className="stage-flow">{stages.map((s,i)=><div className="stage-wrap" key={s.key}><div className={`stage-glass ${s.state}`}><div className="stage-icon-glass"><StageIcon type={s.key}/></div><div className="stage-number">0{i+1}</div><strong>{s.title}</strong><small>{s.key}</small><span className="stage-status">{s.state==="done"?"✓ تکمیل شد":s.state==="active"?"● در حال انجام":s.state==="error"?"! خطا":"○ در انتظار"}</span></div>{i<stages.length-1&&<div className={`flow-line ${s.state==="done"?"done":""}`}/>}</div>)}</div><div className="activity"><div><b>🔵 فعالیت Agent</b><p>{message||(project?`مرحله «${active?.title||project.stage}» در حال پیگیری است.`:"یک ایده وارد کنید؛ Factory بقیه مراحل را خودکار انجام می‌دهد.")}</p></div><div className="metrics"><span>⏱ {time}</span><span>📁 {project?.execution?.status||"READY"}</span><span>⚙️ {jobId?`Job: ${jobId}`:"Durable Worker"}</span><span>⚠️ {project?.error||"0 خطا"}</span></div></div></section><div className="grid"><section className="card wide"><div className="label">AUTONOMOUS COMMAND</div><h2>چه چیزی می‌خواهید بسازید؟</h2><div className="command"><input value={command} onChange={e=>setCommand(e.target.value)} onKeyDown={e=>e.key==="Enter"&&runFactory()} placeholder="مثلاً یک اپ مدیریت کافینت بساز..."/><button className="primary" disabled={loading||!command.trim()} onClick={runFactory}>{loading?"در حال ثبت Job…":"ساخت خودکار 🚀"}</button></div><p>یک فرمان کافی است؛ Job در Backend ثبت می‌شود و Worker مستقل ادامه کار را حتی با بسته‌شدن صفحه انجام می‌دهد.</p></section><section className="card"><div className="label">مرحله فعلی</div><div className="value">{active?.title||(project?"تکمیل":"—")}</div></section><section className="card"><div className="label">Project ID</div><div className="value small-value">{project?.projectId||"—"}</div></section><section className="card"><div className="label">QA Gate</div><div className="value">{project?.execution?.qa?(project.execution.qa.passed?"✓ PASS":"✕ FAIL"):"—"}</div></section><section className="card" id="license"><div className="label">License Center</div><h3>LIFETIME</h3><p>دستگاه‌ها: 1 / 2<br/>کانال‌ها: 1 / 1</p></section><section className="card full" id="build"><div className="label">ANDROID BUILD CENTER</div><div className="build-row"><div><h2>{build?`Build #${build.buildId}`:"ساخت APK"}</h2><p>{build?`Codemagic: ${build.status||"queued"}`:"پس از عبور از QA، Build واقعی شروع می‌شود."}</p>{build?.artifacts?.length?<p>Artifact: {build.artifacts.map(a=>a.name||a.type||"artifact").join("، ")}</p>:null}</div><button className="primary" disabled={!canDownload} onClick={makeArtifactLink}>{artifactLink?"لینک آماده است":"دریافت لینک APK"}</button></div>{artifactLink&&<p><a href={artifactLink} target="_blank" rel="noreferrer">⬇️ دانلود APK</a></p>}</section></div></section></main>;
}
